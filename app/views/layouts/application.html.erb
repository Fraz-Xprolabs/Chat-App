<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Hotwire Chat" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen flex flex-col">
    <header class="bg-white shadow px-5 py-3 flex justify-between items-center">
      <h1 class="text-xl font-bold">Hotwire Chat</h1>

      <nav>
        <% if user_signed_in? %>
          <%= button_to "Logout", destroy_user_session_path, method: :delete,
              class: "text-red-500 hover:text-red-700 font-medium" %>
        <% else %>
          <%= link_to "Login", new_user_session_path,
              class: "text-blue-500 hover:text-blue-700 font-medium" %>
        <% end %>
      </nav>
    </header>

    <main class="container mx-auto mt-6 px-5 flex-1">
      <%= yield %>

      <% if notice %>
        <p class="mt-4 p-2 bg-green-100 text-green-800 rounded"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="mt-4 p-2 bg-red-100 text-red-800 rounded"><%= alert %></p>
      <% end %>
    </main>
  </body>
</html>
